global:
  scrape_interval: 15s # 15초마다 데이터 수집

scrape_configs:
  - job_name: 'mmt-server'
    metrics_path: '/actuator/prometheus' # 데이터를 가져올 경로
    static_configs:
      - targets: ['host.docker.internal:8080'] # 도커 컨테이너 내부에서 호스트(내 컴퓨터)의 8080 포트 접근